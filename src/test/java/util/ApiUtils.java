package util;

public class ApiUtils {

/*Servislere özel metotların bulunduğu yardımcı parametreli metotlar(listede döndürme vs)

public static ClassIsmi metot(Response response, parametre) {

    List<ClassIsmi> değişken = metotisim(response).response class değişken ismi

    for (Product item : products) {
        if (item.getId().equals(productId)) {
            return item;
        }
    }
    return null;
}
    */
	
}
